#include <stdio.h>

int main() {
  int n, h;
  float s;

  scanf("%d", &n);
  scanf("%d", &h);
  scanf("%f", &s);

  int numero = n;
  float salario = h*s;

  printf("NUMBER = %d\n", numero);
  printf("SALARY = U$ %.2f\n", salario);

  return 0;
}
